<% eid ||= 'paymentPackage' %>
<select id="<%= eid %>" name="<%= eid %>" class="form-control">
  <% CocsProxy.instance.getPaymentPackagesInfo().sort_by{|k,v| v['_startTime']}.reverse.each do |sid, value|%>
    <% if sid =~ /^Store.+/ %>
      <option value='<%= sid %>'>
        <%= "#{sid} : #{value['name']}" %>
      </option>
    <% end %>
  <% end %>
</select>