defmodule KfGameUtilsTest do
  use ExUnit.Case

  alias KfGame.Utils

  test "call_data_server" do
    # player_id = 'p1'
    # data = Utils.call_data_server(nil, :test_rpc, [player_id])
    # assert player_id == Dict.get(data, "id")
  end

  # test "cast_data_server" do
  #   player_id = 'p1'
  #   Utils.cast_data_server(nil, :test_rpc, [player_id])
  # end

end
